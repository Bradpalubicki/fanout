# Fanout correction and API exposure plan — 2026-09-23

Status: PLAN ONLY. CX recommendation; exact-artifact independent CC review required. No product edits, migrations, deployment, credentials changes, queue emission, or live provider actions authorized. Plan approval does not authorize implementation.

## Frozen basis and reconciliation

Requested diagnosis reference: 15c2fdcedf3ffde6e945e827471980e5bcfda5ae. Current HEAD independently read twice: 7dd7caea6abf1636484a96a80a0d0bb4c1b4a4cf; 22 descendant commits. The supplied SHA is a product-source commit (integration-status change), not a standalone diagnosis artifact. This plan rechecks the reported diagnosis against source and Git; it does not treat commit messages or repository Markdown as behavioral proof. Original diagnosis narrative completeness is UNPROVEN.

Existing dirty files preserved: BIBLE-UPDATE-NEEDED.md, src/distributors/twitter.ts, src/distributors/linkedin.ts. Twitter worktree SHA256 911848D24633C97785E1B363D58CFE9BA1C9D42D0A2F7B1161479B25387D11D6; LinkedIn D4945A05D43F9D3C273D2A2E92F37BB36983503146B3140F29B8B6C485C27C09. These are concurrent, unapproved implementation changes, included as exposure risks, not credited as completed packages. Review reopens on HEAD/source drift.

Source reconciliation:
- b4f9a5f added biolink page ownership before click reads (src/app/api/dashboard/biolink/route.ts:45-55), rejects Meta DM-to-comment transport (inbox/route.ts:149-167), orders post snapshots (src/app/api/v1/analytics/[postId]/route.ts:52-56), and uses latest mobile snapshots (src/app/api/mobile/analytics/route.ts:56-77). Keep these corrections; additional boundaries below remain.
- bb2d57b + b5fc352 + provider commits now implement profile-filtered native-history reads and a backfill worker: src/app/api/v1/history/route.ts:66-102; src/inngest/functions/backfill-history.ts:25-213. History is no longer only a proposed dashboard port.
- 3c522fc/5e7c152/7dd7cae add account metric migration, Facebook/Bluesky adapter methods, collector, and read API: supabase/migrations/019_account_analytics.sql:14-64; src/distributors/facebook.ts:92-138; bluesky.ts:181-217; src/inngest/functions/collect-account-analytics.ts:20-124; src/app/api/v1/analytics/account/route.ts:62-147. Remaining work is correction/completion, not a new table and endpoint from scratch.
- OAuth authorization checks org ownership (authorize/route.ts:38-46); select-page checks ownership before reading tokens (select-page/route.ts:18-25,41-50). Those are session/org boundaries, not a profile-key API design.
- Upload remains a Clerk user upload to a public blob (src/app/api/upload/route.ts:11-38); v1 validation is an inline schema (src/app/api/v1/post/route.ts:11-16). Do not count them as profile-owned upload or complete platform validation.
- Dirty Twitter code fetches arbitrary media URLs and can report successful text publication after all media uploads fail (twitter.ts:101-150). Dirty LinkedIn selects organization author when pageId is supplied (linkedin.ts:7-41). Review separately before either is accepted.

Notion: fetched discovered project page 32e663704e40816fbe7bfea7d8ea9ff4. It is an old unverified Operating Bible, not an approved September checkpoint. Do not use its integration claims as evidence. Approved destination/last joint product checkpoint UNPROVEN. No Notion save claimed; durable session state goes to portfolio. CFC held for this exposure scope.

## Priority corrections before exposure (P0 foundation)

1. Bind durable resources to authority at every stage. verifyApiKey resolves exactly one profile (src/lib/auth.ts:23-43). A key never becomes org authority merely because that profile has org_id. Query resource id AND profile_id before descendants, counts, token lookup, mutation, or queue generation. Mutations repeat the ownership predicate; affected-row count must match expectations.
2. Fix worker resource/token pairing. fanOut currently reads post by id at src/lib/fan-out.ts:52-58 and independently chooses tokens by supplied profileId at61-65. Immediate/scheduled/retry callers pass event values directly (fan-out-post.ts:44-51; scheduled-post.ts:11-22; retry-post.ts:20-31). Actual-source mocked execution reproduced post-B content dispatched with token-A. This is a proven local worker invariant failure; public exploitability/deployed exploitation is UNPROVEN. Reject mismatched job/post/profile/platform tuples before tokens, writes, notifications, webhooks, or providers. Derive destinations and profile from an authorized durable job, validate event assertions, recheck on retries, scheduling, cancellation and reconnection. Profile-initiated jobs cannot use fleet collectors.
3. Correct current adjacent authority flaws, not only future ports. biolink PATCH authorizes the old page then forwards arbitrary rest into update (biolink/route.ts:99-113): allowlist fields, make ownership immutable, test attempted profile reassignment. dashboard generate-content proxies arbitrary body with INTERNAL_API_KEY (generate-content/route.ts:24-36); downstream can write product queue (generate-social-content/route.ts:180-189). Require explicit internal-product authority, never automatic promotion from ordinary org session/profile key. product-profile GET resolves global slug then can create under caller org (social-setup/product-profile/route.ts:31-56); restrict to intended internal authority and provisioning operation.
4. Fail closed when machine secrets are missing. cron/process-queue/route.ts:8 accepts literal Bearer undefined when CRON_SECRET is unset; generate-social-content/route.ts:55-57 compares interpolated optional secrets. Audit sibling cron/internal guards, require configured nonempty secrets, and test profile/admin/session credentials against each authority. This is a conditional source defect; deployed env remains UNPROVEN.
5. Complete private/public dispatch protection now. Meta has type guard, but Twitter/YouTube/LinkedIn public replies are selected only by platform (dashboard/inbox/route.ts:168-220). Reject dm and unknown types for every public transport before token decryption/provider calls. Never mark replied solely from caller status. Keep DM transport disabled until its own gate passes.
6. Protect queue ownership and provider accounting. Scope idempotency keys by profile, platform, resource and operation; track attempted/delivered/unknown separately. Provider success followed by DB failure must not cause an automatic duplicate send. No token/plaintext credentials in response, logs, or persisted step outputs. Select-page currently returns provider page objects including access_token (select-page/route.ts:63-70); return a safe projection and retain credentials server-side.

## Authority contracts

| Authority | Permitted scope | Must not inherit |
|---|---|---|
| Profile key A | Explicit capabilities for profile A only; current org entitlement may be read to permit A work | B sibling, C foreign org, provisioning/key rotation, org settings/billing, internal products, fleet sync |
| Clerk org session | Existing authorized organization journeys, with explicit role checks for privileged actions | Global products/admin/other orgs; never accepted as profile-key fallback |
| FANOUT_ADMIN_KEY | Existing provisioning contract only (v1/profiles/route.ts:36-49,67-102) | Automatic general data/read/reply access; separate allowlist and audit |
| Internal product authority | Named permitted products and actions | Customer profile authority, arbitrary caller product slug |
| Cron/signed worker authority | Scheduled fleet jobs with explicit server provenance | Unvalidated external events or profile-triggered fleet work |

Do not replace dashboard auth. Add narrow v1 routes/services and preserve dashboard/member/admin behavior. Denied requests may create only caller-scoped rate-limit/security audit telemetry, never business/subscription/resource/queue/provider effects. Move B/C target rejection ahead of getOrCreateOrgSubscription (post route.ts:55-66; schedule route.ts:56-66; subscriptions.ts:36-44).

## Package scope and remaining effort

Estimates are engineering judgments in focused engineer-days (about 6 productive hours/day), including implementation, local fixtures, failure recovery and one independent review correction cycle. They exclude provider approval wait, deployment, credential procurement, broad UI redesign, and full multi-platform parity. They are not a delivery commitment. Work already in HEAD is excluded from remaining estimates. Re-estimate each package after its first behavioral fixture; do not sum as a calendar promise.

| Package | Remaining scope | Effort breakdown / total | Exit falsifier |
|---|---|---|---|
| P0 Authorization foundation | Corrections above; real key resolver A/B/C harness; worker provenance; authority separation | boundaries 2-3d + adjacent corrections 2-4d + adversarial integration/review 2-3d = 6-10d | Any wrong-tenant read/count/write/token/queue/provider effect, or profile promotion to org/admin/product authority |
| H History | Complete native-history lifecycle using existing API/table/adapters; initial trigger + incremental refresh, robust pagination, truthful sync status, provenance/reconciliation | ownership/lifecycle 1-2d + pagination/recovery/reconciliation 2-3d + fixtures/review 1-2d = 4-7d | A sees B/C; cursor skips equal timestamps; repeated empty cursor loops; stale/failed sync claims complete |
| C Comments | Profile-scoped list/count/detail/read-state/reply; finish collector pagination/error states and target identity for initial Facebook/Instagram slice; capability-deny other transports until separately proven | service/API 1-2d + collector/reply/idempotency 2-4d + fixtures/review 2-3d = 5-9d | B/C item accepted, B/C token selected, false replied state or duplicate provider reply |
| D DMs | New private conversation/message model and one selected provider's ingestion/send lifecycle; provider feasibility checkpoint before construction | feasibility 1-2d, then conditional model/ingest 3-5d + send/recovery 3-5d + adversarial tests/review 2-3d = 9-15d if feasible | Any DM enters public endpoint; foreign conversation/recipient accepted; failure reports delivered |
| A Account analytics | Correct and finish existing Facebook/Bluesky metrics path; profile-scoped reads, internal fleet collection, account identity/period/grain/unknown-value semantics | metrics/data contract 1-2d + collector/recovery 1-2d + API/fixtures/review 1-2d = 3-6d | B/C metrics/tokens, profile request invokes fleet collection, unknown period shown as zero, wrong-date/double-counted totals |
| U Upload | New profile-owned media API using reusable blob transport, durable asset records, quotas, safe ingestion, post ownership integration and cleanup | ownership/storage contract 1-2d + transport/failure/cleanup 1-2d + fixtures/review 1-2d = 3-6d | B/C asset attach/read/manage allowed; unauthenticated storage write; spoofed type/oversize/unsafe URL accepted |
| V Validation | Shared versioned per-platform/capability validation for validate, post, schedule and worker dispatch; structured errors and no-write preflight | rule inventory/contract 1-2d + integration 1-2d + fixtures/review 1-2d = 3-6d | validate accepts something submit rejects under same evidence, unknown platform accepted, invalid input causes business/provider effects |

DM provider choice defaults to feasibility research for Meta business messaging, not a claim of permission or capability. Each additional provider for H/C/D/A requires a separately estimated adapter/capability package after official documentation and controlled fixtures. Existing method names do not establish provider support.

## Package details, data and acceptance

H — Keep native account history distinct from local delivery records. Existing reads filter external_posts and sync state by A (history/route.ts:72,102), but timestamp-only cursor at75-81,94 needs a stable tuple tie-breaker and null-time policy. Backfill uses rows imported to infer pages at180-184, so use explicit page budget/repeated-cursor detection. Check DB errors when loading/saving state, write rows and cursor atomically or replay safely; stop condition distinguishes exhausted, partial cap, unsupported, failed, never-started and stale. Account reconnection gets an identity/version so old cursors and old-account rows cannot masquerade as the replacement account. Reconcile successful Fanout publications by provider natural key rather than overwriting origin='native' (backfill-history.ts:156-174). Source event search lists only backfill consumer/self-reemit, so initial producer/incremental workflow is UNPROVEN and included. Start with existing Facebook/Instagram/Twitter/Bluesky adapter contracts, expose only individually verified capabilities. Given an A sync and interleaved B/C rows, all pages/counts/statuses remain A; crash/replay/duplicate/zero-row pages preserve progress and exact records. File impact: v1/history, backfill-history, oauth callback/select-page/credential connectors, worker registration, distributors, migrations018 successor, history tests.

C — dashboard inbox reads/counts are intentionally org-wide (inbox/route.ts:18-42); PATCH owns by org and uses item's token (65-85). New profile API must constrain id AND key profile before loading descendants/token, including AI suggestions; suggest-reply currently trusts client fields and invokes Anthropic (suggest-reply/route.ts:6-11,26-47). Load A's stored item if suggestions are included, bound spend and never auto-send. Collector has bounded recent windows and catches provider errors as empty arrays (collect-inbox.ts:29-34,61-62,71-75,102-103,120-128); model partial/error/stale separately. Persist provider comment id/parent post/author identity explicitly; verify YouTube thread-vs-comment mapping (collect-inbox.ts:264-274 versus inbox reply:195-197) and LinkedIn person/company actor before expanding beyond initial Meta scope. All supported message types have explicit transports. Atomic delivery intent + idempotency + unknown-outcome reconciliation covers send-success/persist-failure. Given A reply to B/C id, return nondisclosing404 and zero token/provider/business-write/queue operations; A succeeds once and unread count tracks the same scope/filter. File impact: new v1/comments routes, inbox service/dashboard adapter, collect-inbox, migrations for delivery/state uniqueness, API/worker tests.

D — Explicitly separate comment and DM APIs, tables/conversation type, capabilities, recipient binding, retention/access auditing, webhook signature verification, replay prevention and delivery states. Collector writes shown at collect-inbox.ts:49,90,142,202,269 are comment/mention observations, not proof of private-message support. Keep DMs unavailable with explicit unsupported status until chosen provider, required permissions, allowed recipients/windows, webhook provisioning and retention are documented and sandbox-proven. Given A conversation send, recipient is derived from stored A conversation; injected B/C conversation/provider id is denied before any call. Given unsupported or provider rejection/timeout, no public comment and no false sent state. File impact: new conversations/messages services/API, provider-specific adapters and webhook receiver, migration, private inbox UI if exposing it, tests. DM retention and first provider are unresolved product decisions before D construction; feasibility work remains bounded.

A — Existing collector scans all oauth_tokens (collect-account-analytics.ts:28-33) and uses token.profile_id on writes (96-112): internal fleet authority only. A read must never fire this event as an A-scoped refresh; if later requested, implement a separate scoped job. Preserve account identity, provider reported period start/end/timezone, collection time, and connection generation. Facebook adapter currently picks values[0] without period end (facebook.ts:122-135); collector relies on collected_for default (migration019:46); API sums null as0 (account/route.ts:122-126). Return unknown/partial coverage, not fabricated zeros; reach is not generally additive unique reach, so expose daily series and clearly defined sums only when metric semantics justify them. Reject nonfinite/negative invalid counters, isolate one failed account without blocking later accounts, persist per-account failures/retry. Given repeated collection for same actual period, one row remains; A follower400->410 yields current410/change10; missing metrics remain unknown. File impact: account API/collector, base AccountMetrics, Facebook/Bluesky, migration019 successor, tests. Other providers remain capability-unverified.

U — Preserve session upload compatibility via separate adapter; new /api/v1/upload derives A from key, not userId/path. Store opaque media id, profile_id, verified type/size, lifecycle and storage reference; attach by owned media id, not trusting public URL as ownership proof. Public distribution derivatives are intentionally public; private originals and API metadata/control remain protected. If public bytes for uploaded assets are unacceptable, require private storage + short-lived provider URLs before implementation. Validate actual bytes/type and streamed size, safe names, bounded dimensions/duration, profile quota, cleanup and rollback on storage-success/DB-failure. Disable remote URL ingestion initially; existing posting paths that fetch arbitrary media need shared egress guard (private/link-local/loopback/DNS/redirect protection), bounds and timeout before accepting untrusted URLs. Given A upload, only A may attach/manage; B/C ids denied before storage/provider fetch; multipart string/malformed/oversize/spoof input is 4xx with zero storage calls. File impact: new media service/v1 upload, existing upload/compose compatibility adapter, post/schedule schemas, media-fetching distributors, migration and tests. Dirty Twitter media fallback must become explicit failure/partial state, not silent text-only success.

V — A validation service is not an auth swap. Actual inline schema accepts unknown platform, 281-character Twitter text, and Instagram without media; acceptance reproduced locally, not asserted as current provider policy. Official current platform rules must be checked before coding numeric limits. Build one capability/rules registry with policy version, account entitlement/permissions, text counting convention, media requirement/format/count, scheduling timezone/window, supported operation and destination identity. /api/v1/validate returns per-platform valid/invalid/unknown and structured fields; it cannot promise provider acceptance. Reuse rules at post/schedule and dispatch to catch capability/connection drift. Validate all targets before business writes; idempotency/limits shared. Given equal inputs/context/version, validate and enqueue validation agree; malformed JSON/duplicates/unknown targets/B/C media and connection ids fail before business writes, queue, decrypt or provider. File impact: new validation service/API, v1 post/schedule, dashboard/mobile adapters and workers, existing product rules where appropriate without inheriting product authority. UX error/accessibility/responsive verification required for any touched form.

## Mandatory behavioral isolation gate (every package)

Seed A and B in org1, C in org2, distinct keys/hashes, distinct post/comment/DM/media/account/connection records, sentinel tokens and unread counts. Exercise real verifyApiKey/hash code, real handlers/services and workers; DB fixture must enforce predicates and preserve writes. Verify at least one isolated local database/RLS run for migrations before accepting data guarantees. Source regex/filter-call assertions are supplementary.

- Cookie-free A key: A positive read/count/mutation works; swapping every path/query/body/cursor/nested id to B or C yields404 or scope-safe rejection and unchanged B/C state. Unknown keys401. List queries cannot silently widen scope; counts/pagination/errors/status do not leak B/C existence/content.
- Tokens: successful A work uses only A's matching platform+connection generation; no B/C decrypt/read or provider account selection. Missing/revoked/corrupt token fails safely; disconnect/reconnect between queue and execution cannot redirect old work.
- Effects: snapshot business tables, storage operations, AI calls, queue events, provider requests, notifications, webhooks and encrypted/plaintext logs before/after. Denials produce zero business effects. Adversarial queue tuple postB/profileA and reverse, sibling/foreign connection, altered platforms, stale job, canceled/replayed job and duplicate delivery all fail safely; authorized A control still works.
- Separate role controls: an authorized org session may retain its A+B dashboard journey; C denied. Profile key cannot mint/rotate B keys, change settings, provision profiles, run fleet jobs or generate internal-product queue work. Admin/internal/cron keys accepted only by intended routes; empty/unset config never authenticates.
- Failures: DB read/write error, empty lookup, 429/5xx/timeout, provider accepted then DB failed, queue rejection, duplicate webhook, expired scopes, invalid media and concurrent requests. Exact expected status/persistence/effects asserted, not just non-200.
- Mutation gate: deliberately remove each ownership predicate or replace with org scope; substitute B token; bypass type guard; alter event profile. The corresponding behavioral test must fail while A control passes. Never mutate the shared product tree during review; use isolated temporary source transforms.

Any counterexample fails the package; passing unit tests, typecheck or filter spies cannot substitute. Live provider behavior and deployed schema stay UNPROVEN until separately authorized controlled runtime/schema checks. Provider writes require explicit approval of account, payload and cleanup; this plan performs none.

## Sequence and exact checkpoints

G0: independent CC review of this artifact SHA256 + HEAD + dirty source hashes, reconcile any disagreement; no implementation now. G0 fails if scope/authority/estimate/evidence is incomplete.
After a separate build instruction: P0 authorization foundation -> V shared validation -> U owned upload -> H native-history completion -> A account analytics -> C comments -> D feasibility and conditional construction. Each package ends with CX and independent CC approval of the exact source/artifact checkpoint before next package. DM feasibility/documentation research may be planned separately but does not authorize construction. No bulk dashboard conversion.
Exposure is separate from source implementation: package route initially gated; local isolation + migration checks -> controlled staging API/worker journey -> explicitly approved provider fixtures -> narrowly enabled capabilities -> rollout monitoring and disable switch. Disable external routes/jobs on ownership failure; preserve audit evidence, avoid destructive rollback of customer data. No READY claim until the specific deployed journeys are exercised. API contract examples/error copy must match actual supported capability and freshness; touched UI requires rendered desktop/mobile keyboard/error-state checks.

## Executed evidence and reproducibility

Working directory C:/Users/bradp/dev/fanout. Exact whole-repo search:
```powershell
$g=@('--hidden','-g','*.py','-g','*.js','-g','*.html','-g','*.ts','-g','*.tsx','-g','!node_modules/**','-g','!.git/**','-g','!.next/**')
rg -n @g 'verifyApiKey|social/history.backfill|social/account-analytics.collect|inbox_items|/api/upload|/api/v1/history|/api/v1/analytics/account|validatePost|validate-post' .
@(rg --files @g).Count
```
Actual count265. Results traced auth, all v1 key users, inbox/read/reply/collector, history and account workers, compose upload; HTML documentation hits were not treated as behavior. Complete applicable repository roots searched, not just src.

```powershell
git rev-parse HEAD
git rev-list --count 15c2fdcedf3ffde6e945e827471980e5bcfda5ae..HEAD
git log --oneline 15c2fdcedf3ffde6e945e827471980e5bcfda5ae..HEAD
git diff --stat 15c2fdcedf3ffde6e945e827471980e5bcfda5ae HEAD
git diff -- src/distributors/twitter.ts src/distributors/linkedin.ts
```
Actual HEAD as above; count22; tracked delta39 files,4242 insertions,197 deletions. Commit history grouped above; docs/workbooks/ops are not evidence of API closure.

```powershell
rg --files @g | rg 'api[/\\]v1[/\\](comments|messages|dm|upload|validate)([/\\]|[.])'
```
Actual stdout empty; exit1. This proves absence of those named route paths in enumerated source, not absence of every possible equivalent capability. No broad absence claim inferred.

```powershell
rg -n @g 'social/history.backfill' .
```
Actual matches tests/lib/backfill-history.test.ts:28, src/inngest/functions/backfill-history.ts:33 and208. Initial production trigger remains UNPROVEN; plan includes explicit wiring and runtime test.

```powershell
npm test -- tests/api/v1-history.test.ts tests/api/v1-account-analytics.test.ts tests/lib/backfill-history.test.ts tests/security/tenant-scoping.test.ts tests/lib/account-metrics.test.ts tests/lib/list-posts.test.ts
node node_modules/typescript/bin/tsc --noEmit --incremental false
```
Actual tests: 6 files passed,90 tests passed,284ms. Typecheck empty output,exit0. git diff --check: no whitespace errors; existing BIBLE file LF/CRLF warning. These tests use mocked resolver/canned rows (history tests:12-44; account tests:8-23) and source text assertions; full mandatory gate NOT VERIFIED.

Root in-memory TypeScript VM execution of actual fanOut with fake database/provider reproduced `FALSIFIED worker binding: post-B content dispatched with token-A; mocked provider calls=1`. Exact post schema extraction and Zod execution printed `SCHEMA ACCEPTS unknown platform`, `SCHEMA ACCEPTS 281 Twitter chars`, `SCHEMA ACCEPTS Instagram without media`; exit0. No live calls. Reproducible probe saved alongside this plan as fanout-plan-probe-20260923.cjs.

Current verdict: correction/exposure PLAN, product NO-GO for this scope; live providers/deployed schema UNPROVEN. Package progress NOT ESTIMATED until package plan is accepted; no completed implementation packages credited this session. Next authorized action: CC exact-plan review/reconciliation, then present plan to Brad. A separate implementation request is required even if G0 passes.
